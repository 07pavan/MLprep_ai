[pytest]
# Make pytest treat backend/ as the Python root for imports.
# This ensures `tools.pandas_tool`, `utils.validators`, etc.
# resolve to backend/ instead of the root-level stub directories.
pythonpath = backend
testpaths = tests
